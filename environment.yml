name: binder_environment
channels:
  - defaults
  - rdkit
dependencies:
  - python
  - rdkit
  - pip
  - pip:
    - protoc-wheel-0>=3.14.0
    - git+https://github.com/open-reaction-database/ord-schema.git
